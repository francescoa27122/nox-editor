# Desktop walk, 2026-09-06, packaged build of main at f203619 plus #198

Build: `src-tauri/target/release/bundle/macos/Nox.app` from `npm run app:build`
(the DMG step failed after the .app was bundled; the .app is what the walk
drives). Fixture: a two-commit git repository under the session scratchpad,
`rootPath` in `session.json` pointed at it, backup at `session.json.walk-backup`.

| # | Item | Result | Evidence |
|---|---|---|---|
| 1 | Launch by executable path with `--geometry`; the `nox: geometry` line prints | PASS | stdout `nox: geometry 1400x880+0+59`; `ps` comm path and sha256 `0f208e8e…` match the fresh build |
| 2 | Title bar: traffic lights inset, breadcrumb, no overlap | PASS | Screenshot: lights at x 8..69, NOX wordmark starts after them, breadcrumb centred, Commands button right |
| 3 | Native macOS View menu lists Toggle Bottom Panel, Toggle Indent Guides, Toggle Tab Key Moves Focus | PASS | `app_menu list View` returned all three (Focus Menu Bar is listed too, drawn enabled by the native menu as its comment says; pre-existing) |
| 4 | Toggle Bottom Panel opens the bottom panel with a real Terminal tab; the shell prompt appears | PASS | Via the native View menu (the harness cannot send ⌘J): TERMINAL tab active, TASKS beside it, zsh prompt in `walk-fixture`, Restart and Hide on the right, status-bar Terminal item lit |
| 5 | Type `echo walk` in the terminal: each character appears once, output comes back | PASS | Screenshot: `% echo walk` once, `walk` on the next line, new prompt. No doubled characters, so the WebDriver `eecchhoo` was the driver |
| 6 | Switch to the Tasks tab and back: scrollback survives | PASS | Tasks tab shows the empty state with Edit Tasks; back on Terminal the `echo walk` / `walk` lines are still there |
| 7 | Hide closes the panel; Toggle Bottom Panel reopens the view that was showing | PASS | Hide: editor fills the main area, status-bar Terminal item unlit. View > Toggle Bottom Panel: Terminal view back, `echo walk` scrollback intact |
| 8 | Indent guides on a tab-indented file and a space-indented file, aligned to the text | PASS | `tabs.js` detected as Tabs: 2, guides at column 0 and at the tab stop under `console.log`; `spaces.ts` (2-space) one guide per level including the 4-space comment line; the restored Python file (4-space) showed guides at 0, 4 and 8 |
| 9 | Toggle Tab Key Moves Focus: status bar shows Tab Moves Focus; Tab leaves the editor rather than indenting | PARTIAL | The readout appeared in the status bar after the native menu item. The background harness cannot send a Tab keystroke (only Return, Escape, Backspace, Delete, ⌘A), so the focus move itself rests on `tests/tab-focus.test.ts` |
| 10 | Toggle Blame on a committed file shows the fixture author | PASS | Tools > Toggle Blame on `spaces.ts`: leftmost column reads `3e0509f Walk Fixture` on lines 1 to 7 and `04cadcc Walk Fixture` on line 8, the line the second commit added; line 9 (never committed) is blank. Column sits outside the line numbers and fold arrows |
| 11 | Edit > Paste from the native menu pastes clipboard text | UNSEEN | The background harness refuses any menu item that touches the clipboard, and clipboard writes with app locks held. Needs a full-screen session or a human; still open from #187's note (WebView2 there, WKWebView here) |
| 12 | Quit with the terminal open: no shell child survives the process | PASS | Before: `pgrep -P <nox>` listed zsh 9114. Nox > Quit Nox: no `nox` process, `ps -p 9114` gone. A7-009's exit half, seen on a real machine |

Totals: 10 PASS, 1 PARTIAL (9), 1 UNSEEN (11). Session file restored from the backup afterwards.
