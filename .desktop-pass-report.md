# Nox Desktop Pass — 2026-08-28 (Windows 11, dev build)

Walker: Claude, on the operator's own Windows 11 machine. Rust toolchain
installed this session (rustup 1.29.0 / cargo 1.98.0 + VS 2022 Build Tools
17.14.39 with the VCTools workload), which is what made this walk possible at
all — every previous session recorded "cargo is not installed on this machine".

App: `npm run app` (tauri dev). Debug rather than release on purpose: it is the
same `platform.id === 'tauri'` and the same WebView2, it carries devtools, and
`tauri.conf.json` sets no `devCsp`, so **the production CSP applies in dev** —
which is precisely what item W1 is about.

Scope: the six changes merged on 2026-08-28, restricted to what a headless test
structurally cannot reach. Everything a test *could* assert stays a test; walks
are the scarcest resource here.

Legend: PASS / PARTIAL / FAIL / UNSEEN (with reason)

**Method, and why it is not the usual one.** Screen-control consent was
requested and **denied**, so nothing here is driven through the UI. Instead the
walk uses a *probe plugin* as its instrument and `settings.json` as its readout:
a plugin cannot write files, but it can run a command, and `view.toggleTheme`
writes `workbench.theme`. The toggle count is read from the probe's own declared
setting, which makes the same file a readout for `settings.get` too. Everything
below is observed from disk.

Two things the denial itself established, before any item: the allowlist
resolved "Nox" to `c:\users\<admin-user>\appdata\local\nox\nox.exe` — the *installed
0.10.0*, not the build under test, which is the Windows form of the skill's
"`open -a Nox` launches that, not your build"; and the operator's real
`diagnostics.log` already held a `ResizeObserver loop completed with
undelivered notifications.` error from 2026-08-24 — field evidence that the
defect fixed in #162 today was reaching users rather than theoretical.

**Release, not dev, and the reason is the CSP.** In `tauri dev` the renderer's
origin is `http://localhost:1420`; in the packaged app it is Tauri's custom
protocol. `'self'` therefore resolves to a *different origin* in the two, so a
dev-build pass would not have settled W1, which is a claim about the packaged
app.

| # | Item | Status | Evidence |
|---|---|---|---|
| W0 | **Plugins run at all in the packaged app** | **FAIL** | Not on the original list, and it invalidated most of it. A `startup` plugin is spawned, writes its greeting, and is **killed within 2 s**; the handshake then times out and `diagnostics.log` records "*Walk Proc was not started / it did not introduce itself in time*". With the workspace root left empty the same plugin is answered `{"id":0,"ok":true}` **6 ms** after its greeting and lives indefinitely. Cause: `#restartLanguageServers` calls `plugins.stopAll()`, and it runs on **every root change** — including the one at boot when the session restores a folder. `stopAll()` clears the registry rather than restarting it, so plugins stay gone until **Reload Plugins**. **Anyone with a folder open — everyone — has no plugins.** |

**W0 is fixed and re-verified in the same build.** `plugins.stopAll()` moved out
of `#restartLanguageServers` and into `dispose()`, which is what a reload
actually runs and what the old comment was reaching for. Rebuilt the release
bundle and relaunched with the operator's real session restored — the same
folder open that produced the failure — and the probe was answered **63 ms**
after its greeting, survived the full 16 s, and `diagnostics.log` gained
nothing at all. Three tests in `tests/plugin-integration.test.ts` hold it: red
before the change, green after.

The remaining UNSEEN rows stay unseen. They were blocked on W0, and re-running
them needs a fresh walk — which is the right call rather than stretching this
one, since their readout (a plugin running a command) is now available and a
later walk can take them properly.

| W1 | A `.js` plugin runs in a worker under the real CSP | UNSEEN | Blocked by W0: nothing gets far enough to exercise the CSP. The *policy* was separately shown to permit a blob worker in Chromium under the exact header, with a `worker-src 'none'` control proving enforcement was live — but that was `http://localhost`, and the packaged app's origin is Tauri's custom protocol, so `'self'` differs and this stays open. |
| W2 | A theme file in the real config dir appears in Settings and applies | PARTIAL | `themes/probe.json` was read without complaint — no theme problem reached `diagnostics.log`, which a malformed one would have. Whether it *renders* was not observable: screen control was denied and the readout used here is a command a plugin runs, which W0 prevents. |
| W3 | Editing a theme file externally re-applies with no Reload | UNSEEN | Blocked by W0 (the readout depends on a plugin). |
| W4 | Editing `snippets.json` externally is picked up | UNSEEN | Same. |
| W5 | A declared plugin setting reaches the plugin | PARTIAL | The manifest's `settings` block **parsed in the packaged app** — a malformed one is reported, and none was. The value never reached the plugin because of W0. |
| W6 | `plugin-settings.json` edited externally reloads without looping | UNSEEN | Blocked by W0. |
| W7 | A plugin that fails to greet reports promptly | **FAIL (as predicted)** | Timed twice: greeting sent at `07:03:15.920`, error recorded at `07:03:25.893` — **9.97 s**. `#awaitHello` is settled only by a greeting or its timeout, so a plugin killed or crashed at load costs the user ten seconds of silence. |

---

# Nox Desktop Pass — 2026-08-20 (re-run, terminated early by controller scope cut)

Walker: Claude desktop-pass agent. Screen-capture consent granted; app: release Nox.app (fresh launch, then one restart to clear BUG-1).
Fixture: <scratchpad>/nox-walk-fixture (a temporary directory on the walker machine)
Legend: PASS / PARTIAL / FAIL / UNSEEN (with reason)

> **Read this first (added 2026-08-25).** Four of the twelve UNSEEN rows are
> now assertions rather than intentions. Three run against the packaged app on
> all three platforms — **A4**, **C5** and **C8**, in `e2e/specs/walk.e2e.js`.
> **C1** is in `tests/tab-context-menu.test.ts` instead, and *why* is worth
> reading: the WebDriver suite **cannot deliver a right-click**. Tried two
> ways on three engines — `element.click({ button: 'right' })` and the explicit
> W3C pointer sequence — and neither produced a `contextmenu` event on
> WebKitGTK, WebView2 or macOS, while the other ten specs passed on all three.
> So the menu's contents, its shortcut hints and its suppression of the
> webview's own menu are asserted in jsdom, and what stays unverified in CI is
> the gesture alone.
>
> That is the same shape as this walk's own central finding — that the
> verification instrument drops inputs — arrived at against a different
> instrument. It is recorded rather than worked around, because a suite that
> quietly stopped asserting C1 would look identical to one that never tried.
>
> Of the eight rows left: three are style contracts that belong beside
> `tests/component-css-tokens.test.ts` rather than in a WebDriver run
> forbidden from measuring anything (B1, B3, C4), two need a workspace with a
> git repository on disk (C7, C9), one needs a language server on the runner
> (L1), one depends on the tab menu whose gesture cannot be driven (C2), and
> one is palette recency (C6).
>
> This file stays as written. It is the record of what a manual walk could and
> could not do, and that record is the argument for the harness that replaced
> it.

## Verdict summary
2 PASS (A1, C3) · 3 PARTIAL (A2, A3, B2) · 0 FAIL · 12 UNSEEN · **0 app defects** — both bugs raised below were resolved as artifacts of the walk harness, BUG-1 by an instrumented re-walk the same afternoon and BUG-2 on 2026-08-23. Read the two resolution sections, not the raised text above them.

Walk was throttled and finally cut short: the user was continuously active on the machine (iPhone Mirroring); most computer-use batches aborted with "user interrupt". Controller reduced scope at ~10:55 to 5 items / 10 screenshots; all 5 reduced-scope attempts also aborted twice each and were marked UNSEEN per orders.

## Phase A
- [x] A1 Show Changes: diff table borders + real header buttons with hover — PASS. "Git: Show Changes" on modified main.ts: side-by-side diff, "3 changes against the index", red/green row tints, subtle 1px pane/gutter borders (zoomed). Header buttons are real: "Side by side" active state (border + teal tint), "Inline" hover background, "Close" present (but see BUG-1). Clean file shows "No changes — the file matches what the index holds."
- [x] A2 ⌘⇧M opens Problems; rows hover — PARTIAL. ⌘⇧M opened the Problems panel (rail warning icon active). Row hover UNSEEN: panel empty, "Nothing to report. Problems appear here once a language server is running." No LSP connected.
- [x] A3 Dirty tab keeps dot on tab-body hover; ✕ only on close button — PARTIAL. Typed into notes.txt → amber dot appeared on tab. Hovering tab body: dot stays (verified, zoomed). Hovering the close-button/dot position: still showed dot, not ✕, in the single check completed before the scope cut (BUG-2 candidate, unconfirmed — retry batch aborted).
- [ ] A4 Destructive confirm dialog focuses SAFE button — UNSEEN. Never reached: blocked by user-input contention, then scope cut by controller.

## Phase B
- [ ] B1 Problems/References/Notes/Search share one header style — UNSEEN. Only the Problems header ("PROBLEMS", small-caps style) was observed; other panels not visited (contention + scope cut).
- [x] B2 Empty states offer Configure Language Servers / Open Folder / New Note — PARTIAL. Problems empty state DOES offer a "Configure Language Servers" button (seen, styled as a real button). Open Folder / New Note empty states UNSEEN (scope cut).
- [ ] B3 One button family across DiffView/terminal/find — UNSEEN. DiffView buttons seen (consistent family); terminal and find never opened (scope cut).

## Phase C
- [ ] C1 Tab right-click → 7-item menu with hints — UNSEEN, scope cut by controller (both attempts aborted by user-input interrupts).
- [ ] C2 Close Others with dirty tab PROMPTS — UNSEEN, scope cut by controller (depends on C1 menu).
- [x] C3 "index.ts — ui" vs "index.ts — core" — PASS. Zoomed tab strip shows "index.ts — core" and "index.ts — ui" (em-dash + parent-dir suffix). Disambiguation survives app restart.
- [ ] C4 Tab-strip overflow fades + hover scrollbar — UNSEEN, scope cut (never opened enough tabs to overflow).
- [ ] C5 Status-bar EOL click flips LF/CRLF + immediate dirty dot — UNSEEN, scope cut by controller (both attempts aborted).
- [ ] C6 Palette reopened empty shows MRU on top — UNSEEN, scope cut. (Palette itself verified working: opens with ⌘⇧P, filters, footer hints "↑↓ navigate / ↵ select / esc dismiss / > commands / ~ switch file / : line / @ symbol".)
- [ ] C7 Dirty buffer + external disk edit → toast with "Reload from Disk" — UNSEEN. The trigger WAS fired (buffer dirtied, then `echo change >> notes.txt` on disk at ~10:57); both verification screenshots aborted. Note: a "File: Reload File from Disk" command exists in the palette. On-disk notes.txt now has the extra "change" line; buffer edit was discarded at quit.
- [ ] C8 Rail collapse/⌘B/References icon — UNSEEN, scope cut by controller (both attempts aborted).
- [ ] C9 Git gutter marks on modified file — UNSEEN. main.ts was only viewed in the Changes view, never in a normal editor pane before the scope cut.

## LSP
- [ ] L1 LSP-dependent items — UNSEEN. No language server connects in this environment (Problems empty state explicitly says none running; status bar shows "TypeScript" label but Problems reports no server).

## Bugs found

*As raised during the walk. **Both were subsequently resolved as harness artifacts** — see the two resolution sections below. Neither is an app defect; the text here is kept as written so the resolutions have something to answer.*

- BUG-1 ~~CONFIRMED~~ **→ resolved, not an app defect** — Changes view cannot be dismissed. The "Close" header button renders a hover state but clicking does nothing (5+ attempts, cursor visibly on the button, app focused); Escape does nothing; re-running "Git: Show Changes" re-opens rather than toggles; ⌘W closes the underlying FILE (lost utils.ts tab) while the view stays; clicking explorer files merely switches which file's diff shows. Only escape found: restart the app. Also the Changes view hides the tab strip while open.
- BUG-2 ~~CANDIDATE (unconfirmed, 1 of 2 checks)~~ **→ resolved, not an app defect** — On a dirty tab, hovering the close-button position kept showing the dirty dot; ✕ never appeared. Needs a re-check (verification batch aborted by user interrupt).

## BUG-1 resolution (2026-08-20 afternoon, instrumented re-walk)
**Not an app defect. Both dismissal paths were blocked by the walk harness itself.** Verified by rebuilding Nox.app from the same main (ca44580) with a window-level event/signal probe writing to a log file, and re-driving the exact repro on the real desktop app:

- **Close click**: an invisible Claude computer-use harness window (layer 3, screen bounds (972,53)–(1492,773), excluded from the harness's own screenshots by native filtering) covers the top-right of the screen. Synthetic clicks there activate the Claude app and never reach Nox — the probe recorded zero pointer events for clicks on the Close button at its walk-time position, while clicks lower/left (explorer) arrived normally. After moving the Nox window so Close sat outside that region, one click dismissed the view instantly: probe logged `click BUTTON "Close"` → `diffOpen.set(false)` → view unmounted, tab strip back.
- **Escape**: consumed at the OS level by the computer-use environment before any app sees it (its user-abort key). Probe showed ⌘⇧P, typed characters and Enter all delivered; Escape never delivered — with the palette input focused, with a plain div focused, via two injection paths (computer-use and System Events key code 53), and with Magnet quit. A real user's Escape outside a harness session is unaffected; the path is covered by tests (below) and works in the Chromium browser target.
- The remaining BUG-1 observations are current design, not wiring defects: Show Changes deliberately re-opens rather than toggles, ⌘W targets the file because the view is "a lens, not a document" (DiffView.svelte header comment), and the tab strip is hidden because the view takes over the whole editor area. Whether ⌘W should close the lens first is a UX question, not a regression.
- BUG-2 is very likely the same artifact: the notes.txt tab close-button sat at ≈x 981pt — inside the same harness window's bounds — so the hover mousemoves likely never reached Nox either. Re-check with the window moved left of x≈970 or the harness panel accounted for.
- Regression coverage added: `tests/git-diff-view.test.ts` now mounts the full App shell and drives both paths for real — a click on the actual Close button, and an Escape keydown through the keymap `App.svelte` attaches. Both mutation-checked (emptying the button's onclick fails the first; unbinding Escape fails the second).
- Walk-harness guidance for future passes: keep the app window's interactive targets out of the screen region x≥970, y 53–773 (or move/shrink the window first), and never use Escape as a verification input — it is eaten by the harness.

## BUG-2 resolution (2026-08-23)
**Not an app defect either, and closed without a re-walk.** The re-check this report asked for was never run — three sessions of UI work happened in between and nobody came back to it, which is the failure mode the whole "12 UNSEEN" number is about. It is closed instead from the two things that *can* be checked without a Mac:

- **The harness explains it.** The BUG-2 note above already localised the notes.txt close button to ≈x981 — inside the same (972,53)–(1492,773) harness window that was proven to swallow clicks for BUG-1. Hover moves aimed there had the same fate as the Close clicks did. That is the same root cause, one component over.
- **The stylesheet says the opposite happens.** `TabBar.svelte` reveals the glyph on `.close:hover` and `.close:focus-visible` *unconditionally*, and yields the dot under exactly those two conditions; only the tab-wide reveal is narrowed to `:not(.dirty)`, which is what stops the dot and the ✕ painting on top of each other. So the reported state — dot held, ✕ absent, pointer on the button — is not a state these rules can produce.

The behaviour is pure `:hover`/`:focus-visible` over two stacked children, which jsdom cannot evaluate and `tests/support/jsdom-layout.ts` forbids pretending to. It is therefore pinned as a stylesheet contract in **`tests/tab-dirty-affordance.test.ts`**, the way `cursor-affordance.test.ts` pins its rule — because a comment cannot fail. Three assertions, each mutation-checked on 2026-08-23:

| Mutation | Result |
|---|---|
| `.close:hover` narrowed to `.close:hover:not(.dirty)` (the exact reported regression) | red — "expected [ …(4) ] to include '.close:hover :global(svg)'" |
| a third selector added to the dot-yield rule | red — "expected [ …(2) ] to deeply equal [ …(1) ]" |
| `:not(.dirty)` dropped from the tab-wide reveal | red — "expected '.tab:hover .close :global(svg)' to contain '.close:not(.dirty)'" |

**What this does not close.** A stylesheet contract proves the rules say the right thing, not that a real pointer over a real 18px button on real hardware resolves them that way. That claim needs a driven app, and it is one of the seventeen reasons the plan in `docs/superpowers/plans/2026-08-23-production-readiness.md` puts a WebDriver harness ahead of another manual walk.

## Walk log
- 10:00 Quit stale instance, relaunched with fixture; session restored 7 tabs. C3 verified immediately.
- 10:00-10:02 A1, A2, B2(partial) verified via palette-driven Show Changes and ⌘⇧M.
- 10:02-10:52 Heavy contention: user active (iPhone Mirroring frontmost); most batches aborted "user interrupt"; focus repeatedly stolen from Nox. BUG-1 isolated during gaps; Nox restarted at 10:26 to clear the stuck Changes view.
- 10:52 Gap: A3 tested (dot on hover PASS; ✕ swap ambiguous → BUG-2 candidate).
- 10:55 Controller cut scope to 5 items / 10 screenshots / 10 min. All 5 items' batches aborted twice each → UNSEEN per orders.
- ~11:00 Report finalized; Nox quit without saving (fixture buffer change discarded).
